[CookSettings]
; Disable Full GC trigger based on system free RAM (was causing crash during GC on this machine).
; UE default is 2048 MiB; that metric is *available* RAM, not total RAM.
MemoryMinFreePhysical=0
MemoryMinFreeVirtual=0
